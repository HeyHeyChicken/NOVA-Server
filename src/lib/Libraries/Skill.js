class Skill {
  constructor(_main, _settings) {
    this.Main = _main;
    this.Settings = _settings;

    this.InitPermanentSettings();
  }

  InitPermanentSettings(){
  }
}

module.exports = Skill;
